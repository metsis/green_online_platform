	<div class="category">
		<!--		<%= link_to category.name, category %>	-->

		<%= link_to category.name, films_path(:category => category.id) %>

		<%= link_to "+", new_category_path(:parent_id => category) %> 
	</div>

