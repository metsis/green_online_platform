	<div class="category">
		<div class="created_at"><%= category.created_at.strftime("%B %d, %Y") %></div>
		<%= link_to category.name, category %>	
		<div class="actions">
			<%= link_to "+ Add subcategory", new_category_path(:parent_id => category) %> 
		</div>
	</div>

